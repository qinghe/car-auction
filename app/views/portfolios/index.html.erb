<%= render 'users/user_menu' %>
<div class="prepend-1">
<table>
	<tr style="background-color: cyan;">
		<td class="bold">项目名称</td><td class="bold">描述</td><td class="bold">结束时间</td>
	</tr>
  	<% @portfolio.each do |port| %>
	        <tr>
	          <td><%= link_to port.name, project_info_path(port) %></td>
	          <td><%= port.description %></td>
	          <td>
	            <%= escape_date(port.updated_at) %>
	          </td>
	        </tr>
	 <% end %>
</table>
</div>
	 <%= will_paginate @portfolio %>